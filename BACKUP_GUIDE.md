# 디케이자동화 — 다른 PC로 옮기기

이 폴더 전체를 백업하면 자동화 스크립트와 작업 자료를 다른 PC에서 이어서 사용할 수 있습니다.

## 1. 백업하기

1. 실행 중인 Edge 및 자동화 프로그램을 모두 종료합니다.
2. 상위 폴더에서 `디케이자동화` 폴더 전체를 ZIP으로 압축하거나, 외장하드/신뢰할 수 있는 개인 클라우드에 복사합니다.
3. 이 파일과 아래 자료가 포함됐는지 확인합니다.

   - `gw-approval-draft-automation.js`, `gw_login.py` 등 자동화 코드
   - `requirements.txt`, `install_playwright.cmd` 등 설치 파일
   - `디케이 자동화`, `디케이 자동화 로컬저장소` 폴더의 작업 자료
   - `gw_login_config.example.json` (설정 형식 참고용)

## 2. 비밀 정보와 로그인 상태

다음은 개인 정보 또는 로그인 상태를 포함할 수 있으므로, 일반 공유 링크나 공용 저장소에 올리지 않습니다.

- `gw_login_config.json`: 계정 설정이 들어 있을 수 있습니다. 암호화된 USB 또는 개인 암호화 클라우드에만 별도 보관하거나, 새 PC에서 다시 만듭니다.
- `edge-automation-profile`: Edge 쿠키와 로그인 세션이 포함될 수 있습니다. 가능하면 옮기지 말고 새 PC에서 Edge 로그인 및 자동화 로그인 절차를 다시 진행합니다.
- `.env`, API 키, 비밀번호 파일이 추가되어 있다면 같은 원칙을 적용합니다.

## 3. 새 PC에서 복원하기

1. 백업한 `디케이자동화` 폴더를 원하는 위치에 압축 해제합니다.
2. Python과 Node.js를 설치합니다. (자동화 실행에 필요할 수 있습니다.)
3. PowerShell에서 복원한 폴더를 열고 필요한 패키지를 설치합니다.

   ```powershell
   pip install -r requirements.txt
   .\install_playwright.cmd
   ```

4. `gw_login_config.example.json`을 참고하여 필요한 설정을 새 PC에서 입력합니다.
5. 필요 시 `run_gw_login.cmd` 또는 `run-dk-approval-edge.cmd`를 실행합니다.
6. Edge가 열리면 새 PC에서 직접 로그인합니다.

## 4. Codex 대화 이어가기

새 PC에 Codex를 설치한 뒤 **현재와 같은 계정**으로 로그인합니다. 대화 목록이 보이면 해당 스레드를 열어 이어서 작업합니다.

대화 기록만으로는 중요한 맥락이 빠질 수 있으니, 새 작업이나 중요한 결정은 이 폴더의 문서 파일에 함께 기록합니다. 이 파일은 그 최소한의 복구 안내서입니다.

## 5. 백업 확인 목록

- [ ] `디케이자동화` 폴더 전체를 복사했음
- [ ] ZIP 파일 또는 외장하드의 파일을 열어 봤음
- [ ] 비밀 설정 파일을 안전하게 별도 보관했음
- [ ] 새 PC에서 Python, Node.js, Playwright 설치를 마쳤음
- [ ] 자동화 실행 전 새 PC에서 직접 로그인했음
