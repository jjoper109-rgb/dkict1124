
// 메일 폴더 리스트를 조회 합니다.
function mail_folder_list(type) {
  console.group("mail_folder_list(type)");
  console.log("type:", type);
  
  if(type=="private")
  {
    var model = undefined;
  }
  else
  {
    var model = "[]";
  }  

  // 개인 폴더는 트리구조로 되어있어 개인 폴더가 없을 경우 undefined 값으로 할당 해야 합니다.


/*
  switch (type) {
    // 개인폴더
    case "private":
      model = [
        {code: "001", text: "공공일", items: [
          {code: "001-001", text: "공공일-001", unseen: 100},
          {code: "001-002", text: "공공일-002", items: [
            {code: "001-002-001", text: "공공일-002-001"},
            {code: "001-002-002", text: "공공일-002-002"}
          ]},
        ]},
        {code: "002", text: "공공이", unseen: 99},
        {code: "003", text: "공공삼"},
        {code: "004", text: "공공사"}
      ];
      break;
    // 라벨
    case "label":
      model = [
        { code: "label01", text: "라벨01", color: "ad1457", unseen: 100 },
        { code: "label02", text: "라벨02", color: "0b8043" }
      ];
      break;
    default:
      break;
  }
*/

  var parm = "type="+type;
  var urls = "/ajax/"+IMAP_MODE+"boxlistData.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });


  console.groupEnd();
  return model;
}


function mail_folder_unseen(type) {
  console.group("mail_folder_unseen(type)");

  var model = undefined;

  switch (type) {
    // 기본폴더
    case "default":
      model = [
        { code: "allbox", unseen: 0 },
        { code: "inbox", unseen: 1 },
        { code: "sent", unseen: 2 },
        { code: "drafts", unseen: 3 },
        { code: "reserved", unseen: 4 },
        { code: "spam", unseen: 5 },
        { code: "trash", unseen: 6 },
      ];
      break;
    // 개인폴더
    case "private":
      model = [
        { code: "001-001", unseen: 0 },
        { code: "001-002-001", unseen: 0 },
        { code: "001-002-002", unseen: 1 },
        { code: "002", unseen: 2 },
        { code: "003", unseen: 3 },
        { code: "004", unseen: 4 },
      ];
      break;
    // 라벨
    case "label":
      model = [
        { code: "label01", unseen: 0 },
        { code: "label02", unseen: 1 },
      ];
      break;
    case "all":
      model = {
        default: [
          { code: "allbox", unseen: 10 },
          { code: "inbox", unseen: 1 },
          { code: "sent", unseen: 2 },
          { code: "drafts", unseen: 3 },
          { code: "reserved", unseen: 4 },
          { code: "spam", unseen: 5 },
          { code: "trash", unseen: 6 },
        ],
        private: [
          { code: "001-001", unseen: 0 },
          { code: "001-002-001", unseen: 0 },
          { code: "001-002-002", unseen: 1 },
          { code: "002", unseen: 2 },
          { code: "003", unseen: 3 },
          { code: "004", unseen: 4 },
        ],
        label: [
          { code: "label01", unseen: 0 },
          { code: "label02", unseen: 1 },
        ],
      };
      break;
  }

  console.groupEnd();
  return model;
}


// 메일 리스트를 조회 합니다.
function mail_list(folder, min, max, options) {
  console.group("mail_list()");
  console.log("folder:", folder);
  console.log("min:", min);
  console.log("max:", max);
  console.log("options:", options);
  var option_json = JSON.stringify(options);
  console.log("option_json:", option_json);
  var model = undefined;

  /*
  switch (folder){
    case "receiveconfirm" :

      break;
    default:

      var parm = "boxcode="+encodeURIComponent(folder)+"&staridx="+min+"&endidx="+max+"&options="+option_json;
      var urls = "/ajax/"+IMAP_MODE+"listData.php";

            console.log("parm : ", parm);

      $.ajax({
          type: "post", data: parm, url: urls, datatype: "json", async : false,
          success: function (msg) {
            console.log(msg);
            model = msg;
          },
          error: function (e) {
              value = false;
              console.log(e);
          }
      });

      console.log('return model:', model);
      break;
  }
  */

  var parm = "boxcode="+encodeURIComponent(folder)+"&staridx="+min+"&endidx="+max+"&options="+option_json;
  var urls = "/ajax/"+IMAP_MODE+"listData.php";

        console.log("parm : ", parm);

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
        //model = undefined;
      },
      error: function (jqXHR, textStatus, errorThrown) {
        console.log('jqXHR', jqXHR);
        console.log('textStatus', textStatus);
        console.log('errorThrown', errorThrown);
        if (jqXHR.status === 200)
          model = null;
        else
          model = undefined;
      }
  });

  console.log('return model:', model);


  console.groupEnd();
  return model;
}


function mail_view(folder, file, options, virtual) {
  console.group("mail_view(folder, file, options, virtual)");
  console.log("folder:", folder);
  console.log("file:", file);
  console.log("options:", options);
  console.log("virtual:", virtual);


  var model = undefined;

  var parm = "boxcode="+encodeURIComponent(folder)+"&filename="+file+"&options="+JSON.stringify(options)+"&virtual="+virtual;
  var urls = "/ajax/"+IMAP_MODE+"viewData.php";

  console.log("view parm : ", parm);

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (jqXHR, textStatus, errorThrown) {
        console.log('jqXHR', jqXHR);
        console.log('textStatus', textStatus);
        console.log('errorThrown', errorThrown);
        if (jqXHR.status === 200)
          model = null;
        else
          model = undefined;
      }
  });

  console.log('return model:', model);

  if (!folder || !file)
    model = undefined;

  console.groupEnd();
  return model;
}

function mail_emls__download(folder, file) {
  console.group("mail_emls__download(folder, file)");
  console.log("folder:", folder);
  console.log("file:", file);

  var model = [
    { folder: "inbox", file: "1522205796.0000000001", name: "제목없음 001", size: 100 },
    { folder: "inbox", file: "1522205796.0000000002", name: "제목없음 001", size: 200 },
  ];

  console.groupEnd();
  return model;
}

// 메일 다운로드
function mail__download(folders, files) {
  console.group("mail__download(folders, files)");
  console.log("folders:", folders);
  console.log("files:", files);

  var parm = "boxcode="+JSON.stringify(folders)+"&filename="+JSON.stringify(files);
  var urls = "/ajax/"+IMAP_MODE+"pcdownload.php";

  console.log("pcdownload parm : ", parm);

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 쓰기(답장, 전체답장, 전달 시) 내용을 조회합니다.
function mail_compose__search(options, type, folders, filenames) {
  console.group("mail_compose__search(options, mode, folders, filenames)");
  console.log("options:", options);
  console.log("type:", type);
  console.log("folders:", folders);
  console.log("filenames:", filenames);


  var model = {};


  var parm = "options="+JSON.stringify(options);
  var urls = "/ajax/"+IMAP_MODE+"autocomplete.php";

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          console.log(e);
      }
  });

  switch (type) {
    // 다시보내기
    case "resend":
    // 답장
    case "reply":
      /*
      model = {
        to: [
          { personal: "테스터01", address: "test01@test.com" }
        ],
        subject: "답장 테스트",
        body: "<h1>내용없음</h1>"
      }
      */

      var parm = "&boxcode="+encodeURIComponent(folders.join('|'))+"&filename="+filenames.join('|')+"&original="+options.reply_original_message;
      var urls = "/ajax/"+IMAP_MODE+"viewData.php";

      console.log("view parm : ", parm);

      $.ajax({
          type: "post", data: parm, url: urls, datatype: "json", async : false,
          success: function (msg) {
            console.log("model.view = ", msg);
            model.view = msg;
          },
          error: function (e) {
              value = false;
              console.log(e);
          }
      });


      break;
    // 전체답장
    case "replyall":
      /*
      model = {
        to: [
          { personal: "테스터01", address: "test01@test.com" },
          { personal: "테스터02", address: "test02@test.com" }
        ],
        cc: [
          { personal: "테스터01", address: "test01@test.com" },
          { personal: "테스터02", address: "test02@test.com" }
        ],
        subject: "전체답장 테스트",
        body: "<h1>내용없음</h1>"
      }
      */

      var parm = "&boxcode="+encodeURIComponent(folders.join('|'))+"&filename="+filenames.join('|')+"&original="+options.reply_original_message;
      var urls = "/ajax/"+IMAP_MODE+"viewData.php";

      console.log("view parm : ", parm);

      $.ajax({
          type: "post", data: parm, url: urls, datatype: "json", async : false,
          success: function (msg) {
            console.log("model.view = ", msg);
            model.view = msg;
          },
          error: function (e) {
              value = false;
              console.log(e);
          }
      });

      break;
    // 전달
    case "relay":

      var parm = "&boxcode="+encodeURIComponent(folders.join('|'))+"&filename="+filenames.join('|')+"&original="+options.reply_original_message;
      var urls = "/ajax/"+IMAP_MODE+"viewData.php";

      console.log("view parm : ", parm);

      $.ajax({
          type: "post", data: parm, url: urls, datatype: "json", async : false,
          success: function (msg) {
            console.log("model.view = ", msg);
            if (filenames.length === 1)
              model.view = msg;
            else
              model.views = msg;
          },
          error: function (e) {
              value = false;
              console.log(e);
          }
      });

      break;
    // 임시
    case "drafts":
      /*
      model = {
        to: [
          { personal: "테스터01", address: "test01@test.com" }
        ],
        subject: "답장 테스트",
        body: "<h1>내용없음</h1>"
      }
      */

      var parm = "&boxcode="+encodeURIComponent(folders.join('|'))+"&filename="+filenames.join('|')+"&original="+options.reply_original_message;
      var urls = "/ajax/"+IMAP_MODE+"viewData.php";

      console.log("view parm : ", parm);

      $.ajax({
          type: "post", data: parm, url: urls, datatype: "json", async : false,
          success: function (msg) {
            console.log("model.view = ", msg);
            model.view = msg;
          },
          error: function (e) {
              value = false;
              console.log(e);
          }
      });


      break;


  }// switch --


  console.groupEnd();

  return model;
}


/*
// 메일 플래그 변경
function mail_flag_change(folders, files, flag) {
  console.group("mail_flag_change(folders, files, flag)");
  console.log("folders:", folders);
  console.log("files:", files);
  console.log("flag:", flag);
  console.groupEnd();

  var parm = "boxcode="+ folders +"&filename=" + files + "&flag=" + flag;
  var urls = "/ajax/"+IMAP_MODE+"statupdate.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });


  return true;
}
*/


// 메일 플래그 변경
function mail_flag_change(folders, files, flag, options) {
  console.group("mail_flag_change(folders, files, flag, options)");
  console.log("folders:", folders);
  console.log("files:", files);
  console.log("flag:", flag);
  console.log("options:", options);
  console.groupEnd();

/*
  var model = {};
  if (options) {
    if (options.allbox_unseen) {
      model.allbox_unseen = 10;
    }

  } else
    model = true;
*/

  var parm = "boxcode="+ encodeURIComponent(JSON.stringify(folders)) +"&filename=" + encodeURIComponent(JSON.stringify(files)) + "&flag=" + flag + "&options=" + options;
  var urls = "/ajax/"+IMAP_MODE+"statupdate.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          console.log(e);
      }
  });

  console.log("model:", model);
  console.groupEnd();
  return model;
}


// 메일 이동
function mail_move(source_folders, files, target_folder) {
  console.group("mail_move(source_folders, files, target_folder)");
  console.log("source_folders", source_folders);
  console.log("files", files);
  console.log("target_folder", target_folder);
  console.groupEnd();

  if(target_folder=="trash")
  {
    var parm = "command=TRASH"+"&source_folders="+encodeURIComponent(JSON.stringify(source_folders))+"&files="+files;
  }
  else
  {
    var parm = "command=MOVE"+"&source_folders="+encodeURIComponent(JSON.stringify(source_folders))+"&files="+files+"&moveboxcode="+encodeURIComponent(target_folder);
  }

  console.log("parm", parm);

  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });

  return true;
}

// 메일 완전삭제
function mail_delete(folders, files) {
  console.group("mail_delete(folders, files)");
  console.log("folders", folders);
  console.log("files", files);
  console.groupEnd();

  var parm = "command=EXPUNGE"+"&source_folders="+encodeURIComponent(JSON.stringify(folders))+"&files="+files;
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });

  return true;
}



// 메일 라벨 생성
function mail_label__create(code, text, color) {
  console.group("mail_label__create(code, text, color)");
  console.log("code:", code);
  console.log("text:", text);
  console.log("color:", color);

  var parm = "command=LABELCREATE"+"&code="+code+"&text="+text+"&color="+color;
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 라벨 수정
function mail_label__modify(code, text, color, index) {
  console.group("mail_label__modify(code, text, color, index)");
  console.log("code:", code);
  console.log("text:", text);
  console.log("color:", color);
  console.log("index:", index);

  var parm = "command=LABELMODE"+"&code="+code+"&text="+text+"&color="+color;
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}


// 메일 라벨 추가
function mail_label__append(folders, files, labels) {
  console.group("mail_label__append(folders, files, labels)");
  console.log("folders:", folders);
  console.log("files:", files);
  console.log("labels:", labels);

  var parm = "command=LABELAPPEND"+"&folders="+encodeURIComponent(folders)+"&files="+files+"&labels="+JSON.stringify(labels);
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";

  console.log("parm:", parm);


  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });


  console.groupEnd();
  return true;
}

// 메일 라벨 삭제
function mail_label__delete(code) {
  console.group("mail_label__delete(code)");
  console.log("code:", code);

  var parm = "command=LABELDEL"+"&code="+code;
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}


// 메일 라벨 비우기
function mail_label__empty(code) {
  console.group("mail_label__empty(code)");
  console.log("code:", code);

  console.groupEnd();
  return true;
}
// 메일 라벨을 백업 합니다.
function mail_label__backup(code, start, end) {
  console.group("mail_label__backup(code, start, end)");
  console.log("code:", code);
  console.log("start:", start);
  console.log("end:", end);

  var parm = "type=label&boxcode="+encodeURIComponent(code) + "&start=" + encodeURIComponent(start) + "&end=" + encodeURIComponent(end);
  var urls = "/ajax/"+IMAP_MODE+"pcdownload.php";

  console.log("pcdownload parm : ", parm);

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 라벨 삭제
function mail_label__remove(folders, files, labels) {
  console.group("mail_label__remove(folders, files, labels)");
  console.log("folders:", folders);
  console.log("files:", files);
  console.log("labels:", labels);

  var parm = "command=LABELREMOVE"+"&files="+files+"&labels="+JSON.stringify(labels);
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 라벨 인덱스를 변경 합니다.
function mail_label_index__change(codes, indexes) {
  console.group("mail_label_index__change(codes, indexes)");
  console.log("codes:", codes);
  console.log("indexes:", indexes);

  var parm = "command=LABELINDEX"+"&code="+JSON.stringify(codes)+"&index="+JSON.stringify(indexes);
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 발송
function mail__send(params) {
  console.group("mail__send(params)");
  console.log("params:", params);


  var parm = "params="+encodeURIComponent(JSON.stringify(params));
  var urls = "/ajax/"+IMAP_MODE+"sendmail.php";

  console.log("parm:", parm);
  
  var result = false;

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        result = msg;
      },
      error: function (e) {
          console.log(e);
      }
  });

  console.groupEnd();
  return result;
}

// 메일 수신확인 사용자 조회
function mail_receive_confirm_users__search(sendtime) {
  console.group("mail_receive_confirm_users__search(sendtime");
  console.log("sendtime:", sendtime);

  /*
  var model = {
    ukey: "ukey",
    time: "time",
    items: [
      { personal: "테스터1", address: "test1@test.com", received: "2018-01-01 12:10" },
      { personal: "테스터2", address: "test2@test.com", received: 0 },
      { personal: "테스터2", address: "test2@test.com", received: -1 }
    ]
  }
  */

  var parm = "boxcode=receiveconfirm&confirm=Subconfirm&sendtime="+sendtime;
  var urls = "/ajax/"+IMAP_MODE+"listData.php";
  console.log("parm : ", parm);
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });


  console.groupEnd();
  return model;
}

// 메일 발송 취소
function mail_receiveconfirm__cancel_sending(sendtime, addresses) {
  console.group("mail_receiveconfirm__cancel_sending(sendtime, addresses)");
  console.log("sendtime:", sendtime);
  console.log("addresses:", addresses);

  if (!addresses || addresses == undefined) addresses = "";

  var parm = "command=CONFIRMCANCEL&sendtime="+sendtime+"&addresses="+JSON.stringify(addresses);
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";
  console.log("parm : ", parm);
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log("model : ", msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });


/*
  var model = {
    error: 0,  // 0: 성공, n: 에러코드
    cancelsents: 1, // 발송취소 수
    confirms: 0, // 수신확인 수
  };
*/

  console.groupEnd();

  return model;
}

// 메일 발송 취소 삭제
function mail_receiveconfirm__delete(sendtimes) {
  console.group("mail_receiveconfirm__delete(sendtimes)");
  console.log("sendtimes:", sendtimes);

  var parm = "command=CONFIRMDEL&sendtimes="+JSON.stringify(sendtimes);
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";
  console.log("parm : ", parm);
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log("model : ", msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

/*
  var model = {
    error: 0,  // 0: 성공, n: 에러코드
  };
*/

  console.groupEnd();

  return model;
}



// 외부발송보안 승인
//function mail_externalsendsecurity__approval(arg) {
function mail_externalsendsecurity__approval(uniqueidx, sendtime, from) {
  console.group("mail_externalsendsecurity__approval(args)");
  //console.log("args:", args);
  console.log("uniqueidx:", uniqueidx);
  console.log("sendtime:", sendtime);
  console.log("from:", from);

  var model = {};

	var parm = "&uniqueidx="+encodeURIComponent(JSON.stringify(uniqueidx));
  parm = parm + "&writetime="+encodeURIComponent(JSON.stringify(sendtime));
  parm = parm + "&from="+encodeURIComponent(JSON.stringify(from));
  parm = parm + "&gu=complete";
	var urls = "/module/approvalcheck.php";
	console.log('parameter:', parm);
	console.log("urls : "+urls);
	$.ajax({
		type: "post", 
		data: parm, 
		url: urls, 
		datatype: "json", 
		async : false,
		success: function (msg) {
			console.log("msg : ", msg);
			model = msg;
		},
		error: function (e) {
			model.result = false;
      model.error = {
        message: '처리하지 못하였습니다.'
      }
			console.log("error : ", e);
		}
	});

  console.groupEnd();

  return model;
}

// 외부발송보안 반려
//function mail_externalsendsecurity__disapproval(args) {
function mail_externalsendsecurity__disapproval(uniqueidx, sendtime, from) {
  console.group("mail_externalsendsecurity__disapproval(args)");
  //console.log("args:", args);
  console.log("uniqueidx:", uniqueidx);
  console.log("sendtime:", sendtime);
  console.log("from:", from);

  var model = {};

	var parm = "&uniqueidx="+encodeURIComponent(JSON.stringify(uniqueidx));
  parm = parm + "&writetime="+encodeURIComponent(JSON.stringify(sendtime));
  parm = parm + "&from="+encodeURIComponent(JSON.stringify(from));
  parm = parm + "&gu=companion";
	var urls = "/module/approvalcheck.php";
	console.log('parameter:', parm);
	console.log("urls : "+urls);
	$.ajax({
		type: "post", 
		data: parm, 
		url: urls, 
		datatype: "json", 
		async : false,
		success: function (msg) {
			console.log("msg : ", msg);
			model = msg;
		},
		error: function (e) {
			model.result = false;
      model.error = {
        message: '처리하지 못하였습니다.'
      }
			console.log("error : ", e);
		}
	});

  console.groupEnd();

  return model;
}


// 외부발송보안 회수
//function mail_externalsendsecurity__retrieve(args) {
function mail_externalsendsecurity__retrieve(uniqueidx, sendtime, from) {
  console.group("mail_externalsendsecurity__retrieve(args)");
  //console.log("args:", args);
  console.log("uniqueidx:", uniqueidx);
  console.log("sendtime:", sendtime);
  console.log("from:", from);

  var model = {};

	var parm = "&uniqueidx="+encodeURIComponent(JSON.stringify(uniqueidx));
  parm = parm + "&writetime="+encodeURIComponent(JSON.stringify(sendtime));
  parm = parm + "&from="+encodeURIComponent(JSON.stringify(from));
  parm = parm + "&gu=collection";
	var urls = "/module/approvalcheck.php";
	console.log('parameter:', parm);
	console.log("urls : "+urls);
	$.ajax({
		type: "post", 
		data: parm, 
		url: urls, 
		datatype: "json", 
		async : false,
		success: function (msg) {
			console.log("msg : ", msg);
			model = msg;
		},
		error: function (e) {
			model.result = false;
      model.error = {
        message: '처리하지 못하였습니다.'
      }
			console.log("error : ", e);
		}
	});

  console.groupEnd();

  return model;
}



// 메일 플래그 비교
function mail_flag_compare(flags, flag) {
  for (var i = 0, l = flags.length; i < l; i++) {
    console.log("mail flag compare flags : " + flags[i] + " flag : "+ flag);
    if (flags[i] == flag)
      return true;
  }
  return false;
}


// 메일 주소 자동완성 아이템을 로드 합니다.
function mail_address_autocomplete__search(type, search) {
  console.group("mail_address_autocomplete__search(type, search)");
  console.log("type:", type);
  console.log("search:", search);

  var model = undefined;

/*
  switch (type) {
    // 즐겨찾기 주소인 경우
    case "favorites":
      model = [
        {
          name: "즐겨찾기 a",
          address: "a@test.com",
          group: "그룹",
          company: "회사"
        },
        {
          name: "즐겨찾기 ab",
          address: "ab@test.com",
          group: "그룹",
          company: "회사"
        },
        {
          name: "즐겨찾기 abc",
          address: "abc@test.com",
          group: "그룹",
          company: "회사"
        },

      ]
      break;
    // 최근 사용한 주소인 경우
    case "recently":
      model = [
        {
          name: "최근 a",
          address: "a@test.com",
          group: "그룹",
          company: "회사"
        },
        {
          name: "최근 ab",
          address: "ab@test.com",
          group: "그룹",
          company: "회사"
        }
      ]
      break;
  }


  var parm = "type="+ type + "&search=" + search ;
  var urls = "/ajax/"+IMAP_MODE+"autocomplete.php";

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });
*/
  console.groupEnd();
  return model;
}

// 메일 주소 자동완성 아이템을 삭제 합니다.
function mail_address_autocomplete_item__remove(type, address) {
  console.group("mail_address__remove(type, address)");
  console.log("type:", type);
  console.log("address:", address);

  var result = false;

// 즐겨찾기 사용안함 2018-10-18 김승태
//  switch (type) {
//    // 즐겨찾기 주소인 경우
//    case "favorites":
//      break;
//    // 최근 사용한 주소인 경우
//    case "recently":
//		break;
//  }

		var parm = "type="+ type + "&address=" + address ;
		var urls = "/ajax/"+IMAP_MODE+"autocomplete_delete.php";

		$.ajax({
			type: "post", data: parm, url: urls, datatype: "json", async : false,
			success: function (msg) {
				console.log(msg);

				if ( msg.result == true ) {
					result = true;
				} else {
					result = false;
				}

			},
			error: function (e) {
				console.log(e);
				result = false;
			}
		});

  console.groupEnd();
  return result;
}


// 메일 주소록에 저장 합니다.
function mail_addressbook__append(items) {
  console.group("mail_addressbook__append(items)");
  console.log("items:", items);

  console.groupEnd();
  return true;
}

// 메일 즐겨찾기에 저장 합니다.
function mail_favorites__append(items) {
  console.group("mail_favorites__append(items)");
  console.log("items:", items);

  console.groupEnd();
  return true;
}








// 메일 환경설정/개인환경/메일읽기 설정값을 가져옵니다.
function mail_settings_personal_mailread__get() {
  console.group("mail_settings_personal_mailread__get()");

 /*
  var model = {
    mainmenu_type: "LeftTop", // 주 메뉴 타입 (LeftTop: 좌측/상단메뉴 사용, Left: 좌측메뉴(아이콘)만 사용, Top: 상단메뉴(텍스트)만 사용)
    list_type: "List",  // 목록 화면 (List: 기본(리스트), LeftRight: 좌우 분할, TopBottom: 상하분할)
    list_rows: 50,  // 목록 개수 (15, 30, 50, 70, 100)
    new_window: 0,  // 새 창으로 읽기
    link_use: 1,  // 링크 사용
    attached_images_view: 1, // 첨부이미지 보기
    personal_folder_folding: 1, // true: 접기, false: 펴기
    label_folding: 0, // true: 접기, false: 펴기
    attachmentsFolding: 1, // 첨부파일 펼침 (true: 접기, false: 펴기)
  }
*/

  var parm = "gubn=get&type=read";

  console.log("parm:", parm)

  var urls = "/ajax/"+IMAP_MODE+"basicsetting.php";

  console.log("urls:", urls)

  var model = {};

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.log("model:", model)

  console.groupEnd();
  return model;
}

// 메일 환경설정/개인환경/메일읽기 설정값을 저장합니다.
function mail_settings_personal_mailread__set(model) {
  console.group("mail_settings_personal_mailread__set(model)");
  console.log("model:", model)

  var parm = "gubn=set&type=read&options="+JSON.stringify(model);
  var urls = "/ajax/"+IMAP_MODE+"basicsetting.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 환경설정/개인환경/메일쓰기 설정값을 가져옵니다.
function mail_settings_personal_mailwrite__get() {
  console.group("mail_settings_personal_mailwrite__get()");

  /*
  var model = {
    // 보내는 사람 메일 주소
    addresses: [
      { personal: "테스터1", address: "test1@test.com" },
      { personal: "테스터2", address: "test2@test.com" },
    ],
    new_window: 0,  // 새 창으로 쓰기 여부
    cc_folding: 1,   // 참조 접기 여부 (1: 접기, 0: 펴기)
    save_sent_mail: 1, // 보낸메일 저장 여부
    reply_original_message: 1, // 답장시 원문 포함 여부
    recently_used_address: 1,  // 최근에 사용한 주소 여부
    signature: ["ReplyRelay"], // 서명 (Write: 편지쓰기시 서명포함, ReplyRelay: 답장/전달시 기본서명을 포함)
    //signature: [], // 서명 (Write: 편지쓰기시 서명포함, ReplyRelay: 답장/전달시 기본서명을 포함)
    autosave: 60 // 자동저장 (0: 사용안함, 30: 30초, 60: 1분, 180: 3분, 300: 5분)
  }
  */


  var parm = "gubn=get&type=write";
  var urls = "/ajax/"+IMAP_MODE+"basicsetting.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}

// 메일 환경설정/개인환경/메일쓰기 설정값을 저장합니다.
function mail_settings_personal_mailwrite__set(model) {
  console.group("mail_settings_personal_mailwrite__set(model)");
  console.log("model:", model)

  var parm = "gubn=set&type=write&options="+JSON.stringify(model);
  var urls = "/ajax/"+IMAP_MODE+"basicsetting.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
        model = false;
        console.log(e);
      }
  });

  console.groupEnd();
  return true;
}


// 메일 환경설정/메일폴더관리/기본폴더 설정값을 가져옵니다.
function mail_settings_folder_default__get() {
  console.group("mail_settings_folder_default__get()");


/*
  var model = {
    capacity_used: 210, // 사용용량
    remaining_capacity: 290, // 남은용량
    total_capacity: 500, // 전체용량
    items: [
      // code: 폴더코드
      // text: 폴더이름
      // capacity: 용량
      // unseen: 안읽은 메일수
      // count: 총 메일수
      { code: "inbox", text: "받은편지함", capacity: 10, unseen: 100, count: 30 },
      { code: "sent", text: "보낸편지함", capacity: 20, unseen: 100, count: 30 },
      { code: "drafts", text: "임시보관함", capacity: 30, unseen: 100, count: 30 },
      { code: "reserved", text: "예약편지함", capacity: 40, unseen: 100, count: 30 },
      { code: "spam", text: "스팸함", capacity: 50, unseen: 100, count: 30 },
      { code: "trash", text: "휴지통", capacity: 60, unseen: 100, count: 30 },
    ]
  }
*/

  var parm = "command=get&gubn=basic";
  var urls = "/ajax/"+IMAP_MODE+"mailboxmanager.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}

// 메일 환경설정/메일폴더관리/내폴더 설정값을 가져옵니다.
function mail_settings_folder_personal__get() {
  console.group("mail_settings_folder_personal__get()");

/*
  var model = {
    capacity_used: 100, // 사용용량
    remaining_capacity: 400, // 남은용량
    total_capacity: 500, // 전체용량
    items: [
      // code: 폴더코드
      // text: 폴더이름
      // type: 폴더 또는 메일폴더(메일함) (folder: 폴더, box: 메일폴더(메일함))
      // items:
      // capacity: 용량
      // unseen: 안읽은 메일수
      // count: 총 메일수
      { code: "001", text: "공공일", type: "folder", items: [
        { code: "001-001", text: "공공일-001", type: "box", capacity: 10, unseen: 100, count: 30 },
        { code: "001-002", text: "공공일-002", type: "box", capacity: 10, unseen: 100, count: 30 },
      ]},
      { code: "002", text: "공공이", type: "box", capacity: 10, unseen: 100, count: 30 },
      { code: "003", text: "공공삼", type: "box", capacity: 10, unseen: 0, count: 30 },
      { code: "004", text: "공공사", type: "box", capacity: 10, unseen: 0, count: 0 }
    ]
  }
*/

  var parm = "command=get&gubn=user";
  var urls = "/ajax/"+IMAP_MODE+"mailboxmanager.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}

// 메일 환경설정/메일폴더관리/라벨 설정값을 가져옵니다.
function mail_settings_folder_label__get() {
  console.group("mail_settings_folder_label__get()");

  /*
  var model = {
    items: [
      // code: 폴더코드
      // text: 폴더이름
      // capacity: 용량
      // unseen: 안읽은 메일수
      // count: 총 메일수
      { code: "label01", text: "라벨01", color: "ad1457", capacity: 10, unseen: 10, count: 10 },
      { code: "label02", text: "라벨02", color: "0b8043", capacity: 20, unseen: 20, count: 20 },
      { code: "label03", text: "라벨03", color: "9e69af", capacity: 30, unseen: 30, count: 30 },
    ]
  }
  */

  var parm = "gubn=label";
  var urls = "/ajax/"+IMAP_MODE+"mailboxmanager.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}

// 메일 폴더 생성
function mail_folder__create(code, text, type, parent) {
  console.group("mail_folder__create(code, text, type, parent)");
  console.log("code:", code);
  console.log("text:", text);
  console.log("type:", type);
  console.log("parent:", parent);

  var parm = "command=set&gubn=create&text="+encodeURIComponent(text)+"&type="+encodeURIComponent(type)+"&parent="+encodeURIComponent(parent);
  var urls = "/ajax/"+IMAP_MODE+"mailboxmanager.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}


// 메일 폴더 생성
function mail_folder__rename(code, text, type) {
  console.group("mail_folder__rename(code, text, type)");
  console.log("code:", code);
  console.log("text:", text);
  console.log("type:", type);


  var parm = "command=set&gubn=rename&text="+encodeURIComponent(text)+"&type="+encodeURIComponent(type)+"&code="+encodeURIComponent(code);
  var urls = "/ajax/"+IMAP_MODE+"mailboxmanager.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}

// 메일 폴더 삭제
function mail_folder__delete(parent, code, type, options) {
  console.group("mail_folder__delete(parent, code, type, options)");
  console.log("parent:", parent);
  console.log("code:", code);
  console.log("type:", type);
  console.log("options:", options);

  var parm = "command=set&gubn=delete&parent="+parent+"&boxcode="+encodeURIComponent(code)+"&type="+type+"&options="+JSON.stringify(options);
  var urls = "/ajax/"+IMAP_MODE+"mailboxmanager.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });


/*
  var model = {};
  if (options) {
    if (options.allbox_unseen) {
      model.allbox_unseen = 10;
    }

  } else
    model = true;
*/

  console.log("model:", model);
  console.groupEnd();
  return model;
}

function mail_folder__reindex(model){
  console.group("mail_folder__reindex(model)");
  console.log("model:", model);

  var parm = "command=BOXINDEX"+"&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"domailfile.php";

  console.log("parm:", parm);

  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
      },
      error: function (e) {
          console.log(e);
      }
  });

  console.log("model:", model);
  console.groupEnd();
  return true;
}


// 메일 환경설정/자동분류 설정값 조회
function mail_settings_filter__get() {
  console.group("mail_settings_filter__get()");

  var parm = "command=get";
  var urls = "/ajax/"+IMAP_MODE+"filterdata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });


/*
  var model = {
    use: 0,  // 사용여부 (0: 사용안함, 1: 사용함)
    items: [
      // id: 아이디(유니크값)
      // folder: 폴더코드
      // code: 필터코드 (subject: 제목, from: 발신주소, rcpt: 수신주소, domain: 발신도메인)
      // data: 필터값 (필터코드가 subject 이고 필터값이 0인 경우 제목없음, 필터코드가 subject 이고 필터값이 1인 경우 제목이 영문,숫자로만 된 경우, 나머지는 필터 설정값)
      // date: 등록일자
      { id: "0", folder: "inbox", code: "subject", data: "999", date: "2018-01-01" },
      { id: "1", folder: "inbox", code: "subject", data: 0, date: "2018-01-02" },
      { id: "1", folder: "inbox", code: "domain", data: "@naver.com", date: "2018-01-02" },
    ]
  }
*/

  console.groupEnd();
  return model;
}

// 메일 환경설정/자동분류 설정값 저장
function mail_settings_filter__set(model) {
  console.group("mail_settings_filter__set(model)");
  console.log("model:", model);


  var parm = "command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"filterdata.php";
  var value;
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        value = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });


  console.groupEnd();
  return value;
  //return false;
}

// 메일 환경설정/외부메일 설정값 조회
function mail_settings_externalmail__get() {
  console.group("mail_settings_externalmail__get()");


  var parm = "command=get";
  var urls = "/ajax/"+IMAP_MODE+"popmaildata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });
/*
  var model = {
    items: [
      {
        id: "1",
        name: "이름1",
        server: "mail.test.com",
        port: 110,
        userid: "tester",
        userpw: "0000",
        folder: "inbox",
        leave_copy: true, // 서버에 복사본 저장
      },
      {
        id: "2",
        name: "이름2",
        server: "mail.test.com",
        port: 110,
        userid: "tester",
        userpw: "0000",
        folder: "inbox",
        leave_copy: true,
      },
    ]
  }
*/
  console.groupEnd();
  return model;
}
// 메일 환경설정/외부메일 설정값 저장
function mail_settings_externalmail__set(model) {
  console.group("mail_settings_externalmail__set(model)");
  console.log("model:", model);

  var parm = "command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"popmaildata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 외부 메일 자료 가져오기
function mail_settings_externalmail__import(model) {
  console.group("mail_settings_externalmail__import(model)");
  console.log("model:", model);

  var parm = "command=import&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"popmaildata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}


// 메일 환경설정/부재중응답 설정값 조회
function mail_settings_absence__get() {
  console.group("mail_settings_absence__get()");
  var model = {
    use: 0, // 사용여부 (0: 사용안함, 1: 사용함)
    start: "",  // 시작일자 (2018-07-07)
    end: "",    // 종료일자
    // 메시지
    message: "============================================\n"
    + "이 메일은 자동 발송된 부재중 알림 메일입니다.\n"
    + "============================================\n"
    + "관리자(master@uplusgw.com) 님께서는 아래 기간동안 부재중입니다.\n"
    + "부재 기간 : 2017-11-09 ~ 2017-11-09\n"
    + "부재 기간이 끝난 후에 다시 보내주세요.\n"
    + "감사합니다.",
  };

  var parm = "command=get";
  var urls = "/ajax/"+IMAP_MODE+"autoreplydata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}
// 메일 환경설정/부재중응답 설정값 저장
function mail_settings_absence__set(model) {
  console.group("mail_settings_absence__set(model)");
  console.log("model:", model);


  var parm = "command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"autoreplydata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 환경설정/포워딩 설정값 조회
function mail_settings_forwarding__get() {
  console.group("mail_settings_forwarding__get()");
  
  /*
  var model = {
    use: 0, // 사용여부 (0: 사용안함, 1: 사용함)
    leave_copy: 0, // 서버에 복사본 저장 (0: 사용안함, 1: 사용함)
    items: [
      { id: "0", address: "test0@test.com", code: "all", data: undefined, start: "", end: "" },
      { id: "1", address: "test1@test.com", code: "address", data: "1", start: "2018-01-01", end: "2018-12-31" },
      { id: "2", address: "test2@test.com", code: "domain", data: "2", start: "2018-01-01", end: "2018-12-31" },
      { id: "3", address: "test3@test.com", code: "subject", data: "3", start: "2018-01-01", end: "2018-12-31" },
    ],
  };
  */
  
  var parm = "command=get";
  var urls = "/ajax/"+IMAP_MODE+"fowarddata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}
// 메일 환경설정/포워딩 설정값 저장
function mail_settings_forwarding__set(model) {
  console.group("mail_settings_forwarding__set(model)");
  console.log("model:", model);

  var parm = "command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"fowarddata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return true;
}

// 메일 환경설정/서명/개인서명 설정값 조회
function mail_settings_signature_personal__get() {
  console.group("mail_settings_signature_personal__get()");
  console.log("model:", model);

  var model = {
    use: 0, // 사용여부 (0: 사용안함, 1: 사용함)
    selected: "signature2", // 선택된 서명 객체명
    signature1: { subject: "서명1", message: "메시지1" },
    signature2: { subject: "서명2", message: "메시지2" },
    signature3: { subject: "서명3", message: "메시지3" },
  }

  var parm = "gubn=user&command=get";
  var urls = "/ajax/"+IMAP_MODE+"signaturedata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.log("model:", model);
  console.groupEnd();
  return model;
}
// 메일 환경설정/서명/개인서명 설정값 저장
function mail_settings_signature_personal__set(model) {
  console.group("mail_settings_signature_personal__set(model)");
  console.log("model:", model);

  var parm = "gubn=user&command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"signaturedata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}
// 메일 환경설정/서명/전사서명 설정값 조회
function mail_settings_signature_company__get() {
  console.group("mail_settings_signature_personal__get()");

/*
  var model = {
    use: 0, // 사용여부 (0: 사용안함, 1: 사용함)
    message: "전사메시지",
  }
*/

  var parm = "gubn=company&command=get";
  var urls = "/ajax/"+IMAP_MODE+"signaturedata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}
// 메일 환경설정/서명/전사서명 설정값 저장
function mail_settings_signature_company__set(model) {
  console.group("mail_settings_signature_company__set(model)");
  console.log("model:", model);

  var parm = "gubn=company&command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"signaturedata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });
  
  console.groupEnd();
  return model;
}

// 메일 환경설정/수신관리/수신허용 설정값 조회
function mail_settings_receivemngt_writelist__get() {
  console.group("mail_settings_receivemngt_writelist__get()");
  var model = {
    use: 0, // 사용여부 (0: 사용안함, 1: 사용함)
    items: [
      { id: "0", address: "test0@test.com", },
      { id: "1", address: "test1@test.com", },
      { id: "2", address: "test2@test.com", },
      { id: "3", address: "test3@test.com", },
    ],
  };

  var parm = "command=get";
  var urls = "/ajax/"+IMAP_MODE+"whitelistdata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          model = 0;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}
// 메일 환경설정/수신관리/수신허용 설정값 저장
function mail_settings_receivemngt_writelist__set(model) {
  console.group("mail_settings_receivemngt_writelist__set(model)");
  console.log("model:", model);


  var parm = "command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"whitelistdata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          model = 0;
          console.log(e);
      }
  });
  console.groupEnd();
  return model;
  //return true;
}

// 메일 환경설정/수신관리/수신거부 설정값 조회
function mail_settings_receivemngt_blacklist__get() {
  console.group("mail_settings_receivemngt_blacklist__get()");
  
  /*
  var model = {
    use: 0, // 사용여부 (0: 사용안함, 1: 사용함)
    process: "trash", // 수신거부 메일 처리 (delete: 즉시삭제, trash: 휴지동에 저장, spam: 스팸메일함에 저장)
    items: [
      { id: "0", address: "test0@test.com", },
      { id: "1", address: "test1@test.com", },
      { id: "2", address: "test2@test.com", },
      { id: "3", address: "test3@test.com", },
    ],
  };
 */
/*
  var model = {
    use: 0, // 사용여부 (0: 사용안함, 1: 사용함)
    process: "trash", // 수신거부 메일 처리 (delete: 즉시삭제, trash: 휴지동에 저장, spam: 스팸메일함에 저장)
    items: [],
  };
*/
 
  var model = 0;
  var parm = "command=get";
  var urls = "/ajax/"+IMAP_MODE+"blacklistdata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}
// 메일 환경설정/수신관리/수신거부 설정값 저장
function mail_settings_receivemngt_blacklist__set(model) {
  console.group("mail_settings_receivemngt_blacklist__set(model)");
  console.log("model:", model);

  var parm = "command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"blacklistdata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          model = 0;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}

// 메일 수신거부 목록에 저장 합니다.
function mail_blacklist__append(token, addresses) {
  console.group("mail_blacklist__append(token, addresses)");
  console.log("token:", token);
  console.log("addresses:", addresses);

  var parm = "command=add&model="+encodeURIComponent(JSON.stringify(addresses));
  console.log("parm:", parm );
  var urls = "/ajax/"+IMAP_MODE+"blacklistdata.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log("msg:", msg);
        model = msg;
      },
      error: function (e) {
          model = 0;
          console.log(e);
      }
  })

  console.groupEnd();
  return model;
}

// 메일 환경설정/메일업로드 업로드
function mail_settings_mailupload(model) {
  console.group("mail_settings_mailupload(model)");
  console.log("model:", model);

  var parm = "model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"mailupload.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
  //return false;
}

// 메일 환경설정/주소록 조회
function mail_settings_addressbook__get(token, min, max, options) {
  console.group("mail_settings_addressbook__get(token, min, max, options)");
  console.log("token:", token);
  console.log("min:", min);
  console.log("max:", max);
  console.log("options:", options);

  var model = undefined;

  var parm = "command=get&min=" + min + "&max=" + max + "&options=" + encodeURIComponent(JSON.stringify(options));
  var urls = "/ajax/"+IMAP_MODE+"address.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log("msg:", msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });
    
 /* 
  if (min === 1) {
    model = {
      items: [
        {
          id: "1",
          name: "이름1",
          companyName: "회사명1",
          email: "test1@test.com",
        },
        {
          id: "2",
          name: "이름2",
          companyName: "회사명2",
          email: "test2@test.com",
        },
        {id: "3",name: "이름3",companyName: "회사명",email: "test3@test.com",},
        {id: "4",name: "이름4",companyName: "회사명",email: "test4@test.com",},
        {id: "5",name: "이름5",companyName: "회사명",email: "test5@test.com",},
        {id: "6",name: "이름6",companyName: "회사명",email: "test6@test.com",},
        {id: "7",name: "이름7",companyName: "회사명",email: "test7@test.com",},
        {id: "8",name: "이름8",companyName: "회사명",email: "test8@test.com",},
        {id: "9",name: "이름9",companyName: "회사명",email: "test9@test.com",},
        {id: "10",name: "이름10",companyName: "회사명",email: "test10@test.com",},
        {id: "11",name: "이름11",companyName: "회사명",email: "test11@test.com",},
        {id: "12",name: "이름12",companyName: "회사명",email: "test12@test.com",},
        {id: "13",name: "이름13",companyName: "회사명",email: "test13@test.com",},
        {id: "14",name: "이름14",companyName: "회사명",email: "test14@test.com",},
        {id: "15",name: "이름15",companyName: "회사명",email: "test15@test.com",},
        
      ],
      totalCount: 15,
    }
    
  } else {
    
    model = {
      items: [
        {
          id: "101",
          name: "이름101",
          companyName: "회사명101",
          email: "test1@test.com",
        },
        {
          id: "102",
          name: "이름102",
          companyName: "회사명102",
          email: "test2@test.com",
        },
      ],
      totalCount: 300,
    }
  }
  */
  
  console.groupEnd();
  return model;
}


// 메일 환경설정/주소록 저장
function mail_settings_addressbook__set(token, model) {
  console.group("mail_settings_addressbook__set(token, model)");
  console.log("token:", token);
  console.log("model:", model);

/*
  var dumyModel = {
    id: "1",
    name: "이름1",
    companyName: "회사명1",
    email: "test1@test.com",
  }
*/

  var parm = "command=set&model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"address.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });
    
  console.groupEnd();
  return model;
}


// 메일 환경설정/주소록 삭제
function mail_settings_addressbook__delete(token, id) {
  console.group("mail_settings_addressbook__delete(token, id)");
  console.log("token:", token);
  console.log("id:", id);

  var parm = "command=del&id="+id;
  var urls = "/ajax/"+IMAP_MODE+"address.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });
    
  console.groupEnd();
  return model;
}

function mail_user_capability() {
  console.group("mail_user_capability()");

/*
  var model = {
    capacity_used: 210, // 사용용량
    remaining_capacity: 290, // 남은용량
    total_capacity: 500, // 전체용량
  };
*/

  var parm = "";
  var urls = "/ajax/"+IMAP_MODE+"capability.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log("model :", msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });

  console.groupEnd();
  return model;
}



// 그룹웨어 설정값 조회
function groupware_settings__get(model) {
  console.group("groupware_settings__get(model)");
  console.log("model:", model);

  var model = undefined;
  var params = "";

  $.ajax({
    type: "post", data: params, url: "/api/10/groupware_info.php", datatype: "json", async : false,
    success: function (data) {
      console.log("model :", data);
      model = data;
    },
    error: function (e) {
      console.log(e);
    }
  });

  console.groupEnd();
  return model;
}

// 비밀번호 변경
function mail_password__chnage(token, currentPassword, newPassword) {
  console.group("mail_user_capability(token, currentPassword, newPassword)");
  console.log("token:", token);
  console.log("oldPassword:", currentPassword);
  console.log("newPassword:", newPassword);

  var parm = "token="+token+"&oldpasswd="+currentPassword+"&newpasswd="+newPassword;
  var urls = "/ajax/"+IMAP_MODE+"pwdchanged.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log(msg);
        model = msg;
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });
  

  console.groupEnd();
  // 반환값 0: 오류, 1: 성공, 2: 현재 비밀번호와 일치하지 않음
  return model;
}


// 메일 쓰기 자동 저장
function mail_compose__autosave(model, f) {
  console.group("mail_compose__autosave(model, f)");
  console.log("model:", model);

  var parm = "model="+encodeURIComponent(JSON.stringify(model));
  var urls = "/ajax/"+IMAP_MODE+"autosave.php";
  $.ajax({
      type: "post", data: parm, url: urls, datatype: "json", async : false,
      success: function (msg) {
        console.log("retrun:", msg);
        
        // 자동 저장시 UID 가 계속 교체되게 수정함. 2020-11-11 김승태
        //if (model.uid === undefined) {
          f(msg)
        //}        
      },
      error: function (e) {
          value = false;
          console.log(e);
      }
  });


/*
  if (model.uid === undefined) {
    f("123456789");
  }
*/

  console.groupEnd();
}

